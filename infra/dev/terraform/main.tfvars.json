{
    "application_name": "${AZURE_ENV_NAME}",
    "location": "${AZURE_LOCATION}",
    "environment": "${ENVIRONMENT=dev}",
    "service_management_reference": "${AZURE_SERVICE_MANAGEMENT_REFERENCE}"
}
